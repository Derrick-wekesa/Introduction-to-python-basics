#list numbers
list_numbers = [2,4,6,8,10]  
#create function check which accepts one  parameter which is a list
"""
def check(list_numbers):
    # Looping in a list
    for num in list_numbers:
        if num % 2 == 0:
            return "Number is Even"

        else:
            return "Number is Odd"

print(check(list_numbers))

def area_circle(r):
    return 3.14 * r * r
    
r=float(input("Enter the radius of the circle: "))
print(area_circle(r))



def area_triangle(base, height):
    return (base*height)/2



base=float(input("Enter the base of the triangle: "))
height=float(input("Enter the height of the triangle: "))

print(area_triangle(base, height))
"""
def area_rectangle(length, width):
    return (length*width)

area = float(input("Enter the length of the rectangle: "))
area = float(input("Enter the width of the rectangle: "))
print(area_rectangle(area, area))
    



    






