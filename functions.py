# create a new variable called 'computer choice'  and assign it to a string
# a function is a set of code which only runs when it is called
#a function has to have a colon in front of it
#two steps in using a function: definition and execution ; a function happens first then we call it
#executing a function just type the functions name followed by parentheses
#There are two components to declaring a function: the function signature and the function body.

def name_of_function(): # signature
    words = 'function body' # body
    print(words) # body

    #function signature: 





def get_choices ():
 player_choice = "rock"
 computer_choice = "paper"

 return computer_choice

 choices = get_choices()
 print(choices)

                